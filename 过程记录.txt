1.总体顺序:
    1)消去空产生式;
    2)消去单产生式;
    3)消去无用符号(先消去非生成符号，再消去不可达符号);    # 可以不依赖顺序
    4)消除递归(对符号排序后，对该符号消除间接左递归后消除一次直接左递归);
    5)消除左递归后,可能产生空产生式或无用符号，需要再进行处理;
    6)化为greibach范式;
    7)化为npda;
    8)检查语言是否符合npda;